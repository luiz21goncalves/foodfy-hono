{
  "name": "foodfy-hono",
  "version": "0.0.0",
  "scripts": {
    "dev": "bun --env-file=.env.development run --hot src/server.ts",
    "build": "bun build src/server.ts --outdir dist --minify --format esm --splitting",
    "start": "bun run dist/server.js"
  },
  "dependencies": {
    "hono": "4.4.7",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
